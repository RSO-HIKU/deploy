{{- if .Values.azure.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "keycloak.fullname" . }}-azure-kv
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  provider: azure
  parameters:
    usePodIdentity: "false"
    useVMManagedIdentity: "true"
    userAssignedIdentityID: {{ .Values.azure.kvCsiClientId | quote }}
    keyvaultName: {{ .Values.azure.keyVaultName | quote }}
    tenantId: {{ .Values.azure.tenantId | quote }}
    objects: |
      array:
        - |
          objectName: kc-db-host
          objectType: secret
        - |
          objectName: kc-db-name
          objectType: secret
        - |
          objectName: kc-db-user
          objectType: secret
        - |
          objectName: kc-db-password
          objectType: secret
        - |
          objectName: kc-admin-user
          objectType: secret
        - |
          objectName: kc-admin-password
          objectType: secret
{{- end }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: notification-service
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  project: services
  sources:
    # 1) Helm chart from the service repo
    - repoURL: https://github.com/RSO-HIKU/notification-service.git
      targetRevision: test
      path: helm
      helm:
        valueFiles:
          # Pull values from the deploy repo (ref 'values' below)
          - $values/values/test/services/notification-service.yaml

    # 2) Values repo (deploy repo)
    - repoURL: https://github.com/RSO-HIKU/deploy.git
      targetRevision: test
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: services

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
